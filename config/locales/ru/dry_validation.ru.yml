ru:
  dry_validation:
    or: "или"

    errors:
      unique_field?: "уже занято"

      unique_user_field?: "занято другим пользователем"

      record_exists?: "не найдено"

      try_update_operation_later?: "Уважаемый клиент! Вы превысили лимит по количеству попыток изменения данных в переводе"

      unexpected_key: "недопустимый параметр"

      in_range?: "должно содержать от %{min} до %{max} символов"

      array?: "must be an array"

      empty?: "must be empty"

      excludes?: "must not include %{value}"

      excluded_from?:
        arg:
          default: "must not be one of: %{list}"
          range: "Не должно принимать значение от %{list_left} до %{list_right}"

      exclusion?: "must not be one of: %{list}"

      eql?: "must be equal to %{left}"

      not_eql?: "must not be equal to %{left}"

      filled?: "обязательное поле"

      format?: "некорректный формат"

      number?: "must be a number"

      odd?: "must be odd"

      even?: "must be even"

      gt?: "must be greater than %{num}"

      gteq?: "must be greater than or equal to %{num}"

      hash?: "must be a hash"

      included_in?:
        arg:
          default: "Значение должно быть одно из: %{list}"
          range: "Должно принимать значение от %{list_left} до %{list_right}"

      inclusion?: "должен быть один из: %{list}"

      includes?: "must include %{value}"

      bool?: "must be boolean"

      true?: "must be true"

      false?: "must be false"

      int?: "должно быть числом"

      float?: "должно быть числом"

      decimal?: "должно быть числом"

      date?: "must be a date"

      date_time?: "must be a date time"

      time?: "must be a time"

      key?: "обязательное поле"

      attr?: "обязательное поле"

      lt?: "must be less than %{num}"

      lteq?: "must be less than or equal to %{num}"

      max_size?: "size cannot be greater than %{num}"

      max_bytesize?: "bytesize cannot be greater than %{num}"

      min_size?: "используйте от %{num} символов"

      min_bytesize?: "bytesize cannot be less than %{num}"

      nil?: "cannot be defined"

      str?: "must be a string"

      type?: "must be %{type}"

      size?:
        arg:
          default: "size must be %{size}"
          range: "size must be within %{size_left} - %{size_right}"

        value:
          string:
            arg:
              default: "length must be %{size}"
              range: "должно содержать от %{size_left} до %{size_right} символов"

      bytesize?:
        arg:
          default: "must be %{size} bytes long"
          range: "must be within %{size_left} - %{size_right} bytes long"

      uuid_v4?: "is not a valid UUID"

      not:
        empty?: "cannot be empty"